# librecrawl-technical-seo-audit-mcp — runtime dependencies
# The MCP server itself. Everything else in the codebase is Python stdlib.
mcp>=1.2.0,<2.0.0
httpx>=0.27
uvicorn>=0.30
# PDF report pipeline (pdf_report.py): Markdown -> HTML -> PDF
weasyprint>=62
markdown>=3.6
